{
  "name": "interactive-cards",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rsbuild dev --host 0.0.0.0 --port 23080",
    "build": "npm run build:standalone",
    "build:github": "node scripts/build.mjs github",
    "build:standalone": "node scripts/build.mjs standalone"
  },
  "dependencies": {
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.6.4",
    "@rsbuild/plugin-preact": "^1.3.2",
    "typescript": "^5.9.3"
  }
}
